# 🚀 Strapi Docker Setup (Multi-Stage Build)

This project demonstrates how to create a **Strapi v5 application** and run it **locally using Docker** with a **multi-stage Dockerfile**.  
It is tested on **macOS Apple Silicon (M1/M2/M3)**.

---

## 📌 Tech Stack

- **Strapi v5**
- **Node.js 20**
- **SQLite (default database)**
- **Docker & Docker Desktop**
- **Multi-stage Docker build**

---

## 📁 Project Structure

strapi-docker/

├── config/
├── database/
├── public/
├── src/
├── package.json
├── package-lock.json
├── Dockerfile
├── .dockerignore
└── README.md
